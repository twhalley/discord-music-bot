# Oneshot invoked by musicbot-vpn-rotate.timer.

[Unit]
Description=Rotate the music bot's VPN exit
After=network-online.target
Wants=network-online.target

[Service]
Type=oneshot
ExecStart=/usr/local/sbin/musicbot-vpn rotate
